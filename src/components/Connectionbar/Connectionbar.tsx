import React from "react";

import { Button } from "../Button";
import { SidebarWrapper } from "../SidebarWrapper";

export default () => {
  return (
    <SidebarWrapper>
      <Button>Add a connection</Button>
    </SidebarWrapper>
  );
};
